package com.app{	import flash.display.MovieClip;	public class Globals	{		public static var vars:Object = {			gameInited:false,			totalUsers:0,			gameNums:[],			api:"http://api.dartec.mx/cards/",			linea:10,			premio:50,			precio:-10,			players:[],			single: true		};		public static var scope:Array = [];		public function Globals()		{			//contructor		}		public static var randomInt:Function = function (min:uint,max:uint):uint		{		return (Math.floor(Math.random() * (max - min+ 1)) + min);		};		public static var shuffleArray:Function = function (input:Array):Array		{		for (var i:int = input.length-1; i >=0; i--)		{		var randomIndex:int = Math.floor(Math.random() * (i + 1));		var itemAtIndex:Object = input[randomIndex];		input[randomIndex] = input[i];		input[i] = itemAtIndex		}		return input;		};	}}